CONFIG = {
  webpage: {
    title: "2013 MTV Hottest",
    subtile: "This map shows every geolocated Tweet mentioning #MTVHottest since the hashtag began"
  },
  static_map: {
    vizjson: "viz.json"
  },
  dynamic_map: {
      user:'osm2',
      table:'mtvhottest_cleaned',
      column:'timestamp',
      steps: 1250,
      step: 15*60,
      resolution:1,
      countby:'sum(i.retweets + 1)',
      animation_scale: 10,
      ball_size_gain: 1,
      ball_animation_speed: 8,
      balls_color: "rgba(238, 45, 45, .50)"
  },
  cdn_url: "0.tiles.cartocdn.com"
}